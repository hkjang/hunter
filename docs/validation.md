# Hunter 검증 기록

## v1.2.0 목록 탐색·빠른 이동 개선

2026년 9월 12일, 목록 공통 컨트롤과 빠른 이동을 실제 React 화면에서 검증했습니다. 아래는 로컬 준비 검증 결과이며, 커밋별 PostgreSQL 전체 회귀 검사와 이미지 빌드 결과는 [GitHub Actions](https://github.com/hkjang/hunter/actions/workflows/ci.yml)에서 확인할 수 있습니다.

| 검증 | 결과 |
| --- | --- |
| 자동 검사 | 프런트엔드 테스트 26개 통과. TypeScript·Vite 최종 빌드, `go vet ./...`, Go 실행 파일 빌드 통과 |
| 원본 보존 | PentAGI 고정 원본 312파일 SHA-256 일치. 코어·권한·실행 API 변경 없음 |
| 공통 목록 | 데스크톱·모바일 20개 검사 통과. 한국어 표시값·다단어 검색, 이름의 자연 정렬, 숫자·심각도 정렬, 페이지 크기·경계, 새로고침·뒤로/앞으로 이동, 상세 `item` 복귀·이전/다음·닫기 확인 |
| 추가 탐색 | 97개 합성 항목의 페이지 직접 이동, 보고서 숫자 정렬, 기여 점수 정렬·발견 건 상세 연결, 320px 화면 등 12개 검사 통과 |
| 별도 관리 목록 | 사용자·개인 키·감사·에이전트 네 목록을 36개 합성 항목으로 검사. 역할·조직·상태·기간·권한 필터, 날짜·호출 수 정렬, 상세 탭 새로고침 후 원래 목록 조건 복귀 확인. 숨은 키 원문은 검색 대상에서 제외 |
| 로그인·빠른 이동 | 실제 로컬 로그인 후 검색 주소 복귀, 로그인 화면 새로고침, 한영 별칭·초성 검색·키보드 이동, 즐겨찾기·최근 방문의 새로고침 보존, 빈 결과·Esc 검사 통과. 모바일 대화상자 하단 안내 잘림을 수정하고 재검증 |
| 메뉴 권한 표시 | 1512px·390px에서 에이전트 필수 네 권한이 모두 있거나 각각 하나씩 없는 조합 10개 통과. 허용되지 않은 관리자·승인·에이전트 항목은 검색·즐겨찾기·최근 방문에서 숨김. 키보드 순환·Enter·Esc와 빈 결과 무이동 확인 |
| 최종 실행 파일 | Go가 포함한 최종 프런트엔드와 `/services?q=…` 경로 응답 HTML 일치. `/api/health` 버전 `1.2.0` 확인 |
| 전체 화면 | 실제 앱의 데스크톱 24경로·새로고침과 모바일 24경로, 로그인·설정 탭·선택 승인·프로필 버전 확인. 예상 밖 JavaScript·HTTP 오류 없음. 에이전트 상세 다섯 탭 10장과 빠른 이동 4장도 최종 실행 파일에서 캡처 |
| 문서 | 사용자 PDF 31쪽·한글 9,077자·이미지 21개, 관리자 PDF 41쪽·한글 11,678자·이미지 17개. 갤러리 33화면과 모바일 원본 33개, 실제 FAQ 9개와 JSON-LD 일치·버전·canonical·sitemap 검사 통과 |

목록 경계와 정렬 검사는 브라우저에서 API 응답을 합성 자료로 대체했으며, 변경 요청은 차단했습니다. 이는 화면 동작의 검증이고 서버 권한이나 대규모 DB 조회 성능을 대신하지 않습니다. 실제 세션·로그인과 최종 전체 캡처는 로컬 Go API로 별도 확인했습니다. SSO 복귀의 브라우저 분기는 합성 콜백으로 검사했으며 실제 사내 Keycloak realm의 재검증 결과로 표현하지 않습니다.

검색·정렬·화면 페이지 이동은 API가 반환한 자료에 적용합니다. 리소스는 최신 5,000건, 에이전트·감사는 최신 1,000건까지이며 상한에 도달하면 화면 하단에 범위를 표시합니다. 즐겨찾기·최근 방문은 같은 브라우저의 사용자별 저장소에 보관하고 현재 허용 메뉴만 표시합니다.

---

## v1.1.0 PentAGI 코어 통합

2026년 9월 12일, 고정 원본 `ea665308baaff015b226f308438a68d929d0f29b`의 코어를 Hunter에 연결한 결과입니다. 원본 312개 파일의 SHA-256을 대조했으며 원본 수정 없이 별도 bridge와 Hunter 연결 계층을 사용합니다. 세부 출처는 [통합 명세](architecture/pentagi-integration.md)에 기록했습니다.

| 검증 | 결과 |
| --- | --- |
| Go | `go vet ./...` 통과. PostgreSQL을 사용한 전체 45개 상위 테스트 `go test -race -count=1 ./...` 통과(앱 298.304초, 코어 3.438초) 후 긴 한국어 응답 회귀 1개를 추가해 스트림·원본 코어 통합 테스트와 함께 `-race` 재검증 통과(10.583초). 최종 상위 테스트 총 46개 |
| 원본 실행 | 계획 생성, 주 에이전트, 검색·코드 역할 위임, 조언·성찰, 하위 작업 재검토, 보고 흐름 및 실제 원본 SQL 대화 체인 확인 |
| 통제 | 조회 권한 4종, 현재 역할·서비스 접근·개인 키 회전, 실행 한도 축소, 임대 만료, 중지와 완료의 경합, 승인 대기 진단 취소, 임의 명령·대상 변경 거부 통과 |
| 기밀 정보 | JSON 문자열·이스케이프·여러 줄·JWT·청크 경계 마스킹, 한국어 UTF-8 보존, 이벤트·기억 암호화 확인. 장문 응답을 안전한 크기로 나누어 이벤트 저장 과정에서 잘리지 않도록 검증 |
| React | 단위 테스트 10개, TypeScript와 Vite 프로덕션 빌드 통과 |
| 화면 | 로그인 후 24개 경로를 1512px·390px에서 검사하고 새로고침 유지 확인. 로그인·프로필 버전·선택 승인·관리자 설정 탭도 검사. 예상 밖 JavaScript·HTTP 오류와 가로 넘침 없음 |
| 실행 화면 | 화면에서 생성해 Hunter 도구 4종과 모델 13회 호출 후 완료. 같은 목표의 새 실행을 실행 중 중지해 `cancelled` 확인. 상세 5탭의 데스크톱·모바일 검사 통과 |
| 재연결 | 첫 SSE 연결을 3개 이벤트에서 끊고 재연결 시 마지막 이벤트를 중복 전송. `after=0` → `after=3`으로 복구하며 저장된 71개 이벤트가 화면에 정확히 71개 표시됨 |
| 문서 | 사용자 28쪽·관리자 40쪽 PDF, 한국어 텍스트·이미지·HTML 링크·SEO·FAQ 구조 검사. 홍보·갤러리·가이드의 모바일/데스크톱 확인 |
| 재배포 고지 | Go·코어·폰트 143개 구성요소와 npm 운영 의존성 45개를 별도 수집. npm 원문·근거 50개와 출처 2개, manifest 1개. 원문 누락·해시 변조·버전 불일치 시 수집 실패 확인 |

Docker의 `--internal` 네트워크에서 일반 PostgreSQL, 모의 AI와 승인된 합성 HTTP 대상을 사용했습니다. Hunter에는 네 환경변수만 전달하고 UID 10001, 읽기 전용 루트, capability 전체 제거, `no-new-privileges`, 마운트 없음으로 실행했습니다. 원본 코어가 모델을 22회 호출하고 일곱 종류의 Hunter 도구를 실행해 보고서를 생성했습니다. 대상 HTTP 요청은 허용한 경로의 GET 한 번이었고, 외부 HTTP 연결은 차단됐습니다. PostgreSQL 확장은 기본 `plpgsql`만 사용했습니다. 컨테이너 재시작 뒤 실행·작업·진단·발견 후보·AI 설정이 보존됐습니다. 최종 로컬 이미지 `sha256:8011350c0453c58b3628f01f62326410770002882d0b4f695c36bb3b9de9c436`에서 84KB 한국어 한 줄의 SSE 재조합 결과가 원문과 일치했고, 이미지 내 고지 224개 파일과 npm 52개 복사 파일의 SHA-256·크기를 확인했습니다.

이 결과는 로컬 합성 AI를 사용한 실제 코드·도구·저장소·통신 경계의 통합 검증입니다. 실제 LLM의 추론 품질, 취약점 탐지율이나 사내 Keycloak 환경 전체를 검증한 것은 아닙니다. 화면용 합성 데이터·시험 자격증명·모의 서버는 운영 이미지 초기 데이터에 포함하지 않습니다. 원본 Docker 공격 실행기·인터넷 검색·Graphiti 서비스를 기동하는 구성도 아닙니다. AI·에이전트 기능은 관리자 설정에서 기본 비활성화 상태로 시작합니다.

---

## v1.0.0 기존 검증 기록

이 문서는 2026년 9월 11일 릴리즈 준비 과정에서 실제 실행한 검증과 범위를 기록합니다. 화면의 문서용 합성 데이터는 운영 이미지의 초기 데이터에 포함하지 않습니다.

## 자동화·브라우저 검증

| 항목 | 결과 |
| --- | --- |
| Go 정적 검사 | `go vet ./...` 통과 |
| PostgreSQL 통합·동시성 검사 | `go test -race -count=1 -json ./...` 통과: 상위 테스트 30개와 하위 테스트 6개, 실패 0개, 209.024초 |
| React 빌드 | TypeScript 검사와 Vite 프로덕션 빌드 통과 |
| 앱 경로 | 로그인 후 22개 데스크톱 경로와 새로고침 유지 확인 |
| 모바일 | 390px에서 동일한 22개 경로의 가로 넘침 없음; 로그인·승인 화면도 별도 캡처 |
| 추가 화면 | 로그인, 선택적 승인, 모든 관리자 설정 탭, 프로필 메뉴의 버전 표시 확인 |
| 브라우저 오류 | 예상하지 않은 JavaScript·HTTP 오류 0개 |
| 문서 | HTML 링크·SEO 구조·이미지·PDF 확인, 홍보·갤러리·사용자·관리자 페이지 데스크톱/모바일 확인 |

백엔드 검증에는 소유자·팀별 접근 통제, API 키의 현재 권한, 동시 키 회전, 선택적 승인, 대상·범위 정책, DNS/리다이렉트 제한, 작업 임대·취소·권한 변경, 결과 수입·중복, 재검증, 일정, 증거 암호화, OIDC, AI 스트리밍, 외부 발신의 중복 방지 등이 포함됩니다. 독립 재현의 범위와 소스는 [보안 회귀 검증 기록](security-review.md)을 참고하세요.

## 인터넷 경로가 없는 Docker 실행

`docker save`로 만든 `hunter-v1.0.0.tar.gz`를 다시 `docker load`하고, Docker의 `--internal` 네트워크에 PostgreSQL, Hunter 서버, 별도 Hunter 워커와 검증 클라이언트를 연결했습니다. 네트워크의 `Internal=true`와 서버의 기본 게이트웨이 부재를 확인했습니다.

서비스는 `hunter:v1.0.0`, UID/GID `10001:10001`, 읽기 전용 루트 파일시스템, 임시 `/tmp`, 모든 capability 제거, `no-new-privileges`로 실행했습니다. 서비스 프로세스에는 요구된 네 개의 설정 환경변수만 전달했습니다.

1. 상태 API의 버전과 초기 관리자 로그인이 정상 동작했습니다.
2. 신규 DB에서 서비스·발견 건 목록이 비어 있어 예시 데이터가 자동 생성되지 않음을 확인했습니다.
3. 별도 워커가 기본 비활성·미분류 망으로 등록된 뒤 관리자 설정으로 활성화되는 것을 확인했습니다.
4. 승인된 자체 검증 컨테이너의 `/api/health`만 범위에 넣고 실제 HTTP 진단을 실행했습니다. 지정한 망의 별도 워커가 작업을 완료했습니다.
5. 조회 권한 개인 키에는 조회 MCP 도구만 노출되었습니다. 키 회전 직후 이전 키는 거부되고 새 키는 동작했으며 폐기도 완료했습니다.
6. 서비스 컨테이너 재시작 후 로그인, 서비스 설정, 개인 시작 페이지, 자산이 보존되었습니다.
7. 같은 내부 네트워크의 Chromium에서 실제 이미지의 로그인·개인 시작 페이지·중첩 경로 새로고침·로컬 폰트·모바일 화면을 확인했습니다. 외부 자산 요청과 JavaScript 오류는 없었습니다.
8. 기존 DB에 다른 암호화 키를 전달한 별도 컨테이너는 시작을 거부하고 종료 코드 1을 반환했습니다.

압축 아카이브의 Docker manifest에는 `hunter:v1.0.0` 이미지 한 개만 포함됩니다. PostgreSQL, 외부 진단 엔진, 문서 압축 파일은 릴리즈 첨부 자산에 포함하지 않습니다. 게시된 최종 아카이브의 SHA-256은 GitHub 릴리즈 본문에서 확인합니다.

## 검증의 적용 범위

- OIDC는 서명된 토큰을 발급하는 로컬 모의 issuer로 discovery·PKCE·nonce·JWT 검증을 수행했습니다. 조직의 실제 Keycloak realm, 인증서, 프록시와 사용자 정책은 설치 환경에서 연결 확인이 필요합니다.
- AI는 OpenAI 호환 모의 스트리밍 서버로 검증했습니다. 262,144 설정 상한은 연결 모델의 실제 컨텍스트·출력 능력을 보장하지 않습니다.
- 내장 진단은 제한된 HTTP 보안 헤더 점검과 사전 정의한 합성 데이터 권한 비교입니다. 외부 스캐너는 승인된 별도 환경에서 실행하고 결과를 가져옵니다.
- 첫 버전의 목록 조회와 그에 기반한 보고서·대시보드·그래프는 권한 범위 안의 종류별 최신 5,000건으로 제한됩니다.
- 본 검증은 모든 조직의 네트워크 구성과 임의 규모의 동시 부하를 대신하지 않습니다. 실제 서비스 도입에는 관리자 가이드에 따라 사내 연결과 승인 범위를 설정합니다.
